<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team9.projectevaluationslotbooking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectEvaluationSlotBooking">
        <activity android:name=".BookSlotActivity"></activity>
        <activity android:name=".MessageInboxActivity" />
        <activity android:name=".MessageComposeActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegistrationActivity">
            android:label="Registration Page"
        </activity>
        <activity android:name=".LoginActivity">
            android:label="Login Page"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>